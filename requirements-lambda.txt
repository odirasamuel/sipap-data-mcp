# Lambda Layer Dependencies for sipap-data-mcp
# These are the runtime dependencies that will be packaged into the Lambda layer
#
# IMPORTANT: sipap-common and sipap-mcp are provided as SEPARATE Lambda layers
# and should NOT be included here. They are attached to the Lambda function independently.

# Database driver (async PostgreSQL)
# Note: sipap-common already provides redis + hiredis, so we only need asyncpg
asyncpg>=0.29.0

# HTTP client for API-Football direct access (Data MCP redesign 2026-08-19)
aiohttp>=3.9.0
